# Story 1.1: Project Scaffolding

## Status
Done

## Story
**As a** developer,
**I want** a properly configured React + TypeScript project,
**so that** I can start building features immediately.

## Acceptance Criteria

| # | Critério |
|---|----------|
| AC1 | Projeto criado com `npm create vite@latest miplan -- --template react-ts` |
| AC2 | Dependências instaladas: `@dnd-kit/core`, `@dnd-kit/sortable`, `zustand`, `dexie`, `nanoid` |
| AC3 | Tailwind CSS configurado e funcionando |
| AC4 | Estrutura de pastas conforme `docs/architecture.md` criada |
| AC5 | TypeScript types definidos em `src/types/index.ts` (Board, Column, Task) |
| AC6 | App.tsx renderiza "Miplan" como placeholder |

## Tasks / Subtasks

- [x] Task 1: Criar projeto Vite (AC1)
  - [x] Executar `npm create vite@latest . -- --template react-ts`
  - [x] Verificar que projeto inicia com `npm run dev`
- [x] Task 2: Instalar dependências (AC2)
  - [x] Instalar @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
  - [x] Instalar zustand dexie dexie-react-hooks nanoid
- [x] Task 3: Configurar Tailwind CSS (AC3)
  - [x] Instalar tailwindcss postcss autoprefixer
  - [x] Criar tailwind.config.js
  - [x] Adicionar @tailwind directives em src/index.css
- [x] Task 4: Criar estrutura de pastas (AC4)
  - [x] Criar src/components/Board/
  - [x] Criar src/components/Task/
  - [x] Criar src/components/Sidebar/
  - [x] Criar src/components/Import/
  - [x] Criar src/components/ui/
  - [x] Criar src/hooks/
  - [x] Criar src/store/
  - [x] Criar src/db/
  - [x] Criar src/utils/
  - [x] Criar src/types/
  - [x] Criar src/constants/
- [x] Task 5: Definir TypeScript types (AC5)
  - [x] Criar interface Board
  - [x] Criar interface Column
  - [x] Criar interface Task
  - [x] Criar type BoardTemplate
- [x] Task 6: Atualizar App.tsx (AC6)
  - [x] Renderizar "Miplan" como placeholder
  - [x] Verificar build sem erros

## Dev Notes

### Estrutura de Pastas (de architecture.md)
```
src/
├── components/
│   ├── Board/
│   ├── Task/
│   ├── Sidebar/
│   ├── Import/
│   └── ui/
├── hooks/
├── store/
├── db/
├── utils/
├── types/
├── constants/
├── App.tsx
├── main.tsx
└── index.css
```

### TypeScript Definitions
```typescript
export interface Task {
  id: string;
  boardId: string;
  columnId: string;
  title: string;
  description: string;
  order: number;
  timeSpent: number;
  timerStartedAt: number | null;
  createdAt: number;
  updatedAt: number;
}

export interface Column {
  id: string;
  boardId: string;
  title: string;
  order: number;
}

export interface Board {
  id: string;
  name: string;
  type: 'kanban' | 'study-planner';
  createdAt: number;
  updatedAt: number;
}

export type BoardTemplate = {
  type: Board['type'];
  name: string;
  columns: string[];
};
```

### Dependências
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@dnd-kit/core": "^6.1.0",
    "@dnd-kit/sortable": "^8.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "zustand": "^4.5.0",
    "dexie": "^4.0.0",
    "dexie-react-hooks": "^1.1.7",
    "nanoid": "^5.0.0"
  }
}
```

### Testing
- Validar `npm run dev` inicia sem erros
- Validar `npm run build` compila sem erros
- Validar TypeScript sem erros de tipo
- Validar Tailwind classes funcionando

## Definition of Done
- [ ] `npm run dev` inicia sem erros
- [ ] `npm run build` compila sem erros
- [ ] TypeScript sem erros de tipo
- [ ] Tailwind classes funcionando

## Story Points
2

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-31 | 0.1 | Story criada a partir do PRD | River (SM) |
| 2026-01-31 | 1.0 | Story implementada | Dex (Dev) |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References
- Tailwind v4 requires @tailwindcss/postcss plugin (fixed)

### Completion Notes List
- Project created with Vite + React + TypeScript
- All dependencies installed (dnd-kit, zustand, dexie, nanoid)
- Tailwind CSS v4 configured with @tailwindcss/postcss
- Full folder structure created
- TypeScript types defined
- Build passes, dev server starts

### File List
- package.json (created)
- vite.config.ts (created)
- tsconfig.json (created)
- tsconfig.node.json (created)
- index.html (created)
- tailwind.config.js (created)
- postcss.config.js (created)
- src/main.tsx (created)
- src/App.tsx (created)
- src/index.css (created)
- src/vite-env.d.ts (created)
- src/types/index.ts (created)
- src/constants/boardTemplates.ts (created)
- src/components/Board/ (created)
- src/components/Task/ (created)
- src/components/Sidebar/ (created)
- src/components/Import/ (created)
- src/components/ui/ (created)
- src/components/Header/ (created)
- src/hooks/ (created)
- src/store/ (created)
- src/db/ (created)
- src/utils/ (created)

---

## QA Results
*To be filled by QA agent*
